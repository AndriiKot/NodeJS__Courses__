<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/main.css">
    <title>User Page</title>
  </head>
  <body>
    <h1>User: <%= username %></h1>
    <ul>
    <% if(username.toLowerCase() === "andriikot") { %>
      <li>Admin</li>
    <% } %>
      <% hobbies.forEach((hobby) => { %>
      <li><%= hobby %></li>
      <% }); %>
    </ul>
  </body>
</html>
